# Agents.MD

## Project: Bolt Automation Nexus

### Workspace Overview
- Multi-project .NET 9 solution
- C# 13.0
- Key projects: APIClients, Common, FrontEnds, TestDataProvider, Tests, etc.
- Test automation uses Playwright, NUnit, custom logging, and MongoDB test reporting
- Section edit/view all buttons for Property, Exterior, Interior, and Personal Info are modeled as UI fields and constants for robust automation
- UI automation uses field name constants (string) for all field interactions, not UIElement objects, to match helper signatures

### Logging & Reporting
- Logging is handled via NLog, with custom layouts and targets (console, MongoDB, debug, etc.)
- Test results are reported to MongoDB via `MongoLoggingDecorator` wrapping `IAutomationLogger`
- Logging best practices:
  - Use `_logger.StartStep` for logical grouping
  - Use `_logger.Info`, `_logger.Debug`, `_logger.Error` for context-rich messages
  - Use `_logger.LogBusinessRule` and `_logger.LogUiAction` for business and UI actions
  - Remove timestamps from log output by customizing the NLog layout (preferred: no `${date}` or `${longdate}`)

### Test Automation
- Tests are written using NUnit and custom attributes (e.g., `[Tenant]`, `[TestCaseId]`, `[Category]`)
- Test base classes provide dependency injection, browser/page helpers, and logging
- API and UI flows are automated, with strong emphasis on logging each step and assertion
- Test data is provided via static members and custom data providers
- UI section toggles (edit/view all) are automated using field name constants and mapped in DetailsFields and FieldsName
- All field interactions in tests use string field names for compatibility with IPageHelper

### Coding Preferences
- **Primary constructor** is preferred for new C# classes
- Use explicit context and dependency injection
- Prefer clear, step-based, and business-contextual logging
- Use structured and tabular logs for diagnostics when needed

### User Preferences
- Logging should be step-based, business-contextual, and timestamp-free in output
- Code should use primary constructors where possible
- Test code should be readable, maintainable, and leverage all available helpers and logging features
- UI automation should use field name constants for all field interactions

---

*This file is generated by GitHub Copilot, acting as a senior/architect automation engineer, based on workspace analysis and user guidance.*
